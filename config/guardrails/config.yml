models:
  - type: main
    engine: openai
    model: gpt-4

rails:
  input:
    flows:
      - jailbreak detection
      - pii detection
      - topic filtering
  output:
    flows:
      - fact checking
      - hallucination detection
      - harmful content filtering
  dialog:
    flows:
      - conversation flow control
